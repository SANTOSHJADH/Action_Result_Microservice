package com.collection.ActionResult.RepositoryTest;

import static org.assertj.core.api.Assertions.assertThat;

import java.util.Optional;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import com.collection.ActionResult.Entity.ActionEntity;
import com.collection.ActionResult.Repository.ActionRepository;

@SpringBootTest
public class ActionResultRepositoryTest {
	
	/*
	 * @Autowired ActionRepository actionrepository;
	 * 
	 * @Test void isActionAvailable() { Optional<ActionEntity>
	 * actualresult=actionrepository.findById("IC");
	 * assertThat(actualresult).isPresent(); }
	 */
	

}
